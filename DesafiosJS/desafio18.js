// crie um algoritmo que leia o salario de um funcionario e reajuste ele em 15%


let salario = 1000
let bonus = 0.15
let ajuste1 = salario * bonus
let ajusteFinal = salario * (1 + bonus)
console.log(`O seu novo salrio é ${ajusteFinal}R$` )